{
  "name": "warbuddy",
  "version": "0.1.37",
  "private": true,
  "description": "Torn war action queue, shared Dibs, retaliation, and personal target userscript.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/core.test.mjs",
    "check": "npm run test && npm run build"
  }
}
